# Bandit Level 10 → 11

## Goal

Find the password.

## Solution

The password is stored in the file data.txt and is encoded in base64. Use base64 with the -d option to decode the file and display the password.

Command used:

ls
cat data.txt
base64 -d data.txt

## Result

Password found.
